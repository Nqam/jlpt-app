# Plan 4c-2: раздел «Тексты» — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a standalone "Тексты" (reading texts) section — short authored Japanese texts per JLPT level with a Russian translation revealed on demand and 3-4 multiple-choice comprehension questions per text — as a new browse-and-read feature outside the SRS/quiz system, mirroring the existing content-pipeline/screen conventions used for grammar/kanji/vocab.

**Architecture:** Content lives as curated Markdown files (`content/texts/<level>/<id>.md`, frontmatter + `## Текст`/`## Перевод`/`## Вопросы` sections) parsed by a new `scripts/build-content/parse-texts.ts` that mirrors `parse-grammar.ts` exactly, feeding two new tables (`texts`, `text_questions`) in the existing `content.db` build pipeline. `ContentDb` gains `listTexts`/`getText` following the established list/full-row pattern. Two new screens (`TextsListScreen`, `TextDetailScreen`) mirror `VocabListScreen`/`VocabDetailScreen`; the comprehension-question UI is a small self-contained component built directly in `TextDetailScreen.tsx` — it does **not** reuse `Question`/`QuestionView`/`grade()` from the quiz-engine, since those types are shaped around SRS review (`itemType`/`kind`/`ChoiceQuestion` etc.) and comprehension questions are a different, simpler kind of data with no FSRS card behind them. Read-progress is tracked as a plain `texts_read_ids: string[]` setting via the existing `UserDb.getSetting`/`setSetting`, the same pattern `placement_marked_ids` already uses.

**Tech Stack:** No new dependencies. Reuses `gray-matter` (frontmatter), `sql.js` (content.db), React + `react-router-dom`, the existing `Furigana`/`parseRuby` renderer, and the project's existing Vitest/Playwright/ESLint/tsc toolchain.

**Spec:** `docs/superpowers/specs/2026-09-06-plan-4c-2-texts-design.md`

## Global Constraints

- Content sourcing: Tadoku (tadoku.org, にほんご多読 / NPO多言語多読) Level 0 and Level 1 free graded-reader **title lists** are the inspiration for which stories to write, used for the user's own personal, non-commercial, never-redistributed app (explicit, already-made user decision — not to be re-litigated). Tadoku's actual PDF/book text was **not extractable** through available tools (site serves PDF downloads and YouTube read-alongs, no plain-text pages) — confirmed by direct fetch attempts during planning (see Task 2's authoring note). Every one of the 6 texts below is therefore the plan author's **own original Japanese prose** — never a transcription of Tadoku's copyrighted text — attributed honestly in `content/CREDITS.md` per this project's established practice of distinguishing directly-sourced vs. self-authored/inspired-by content (see how `content/grammar`'s N4 そうです entry and `content/CREDITS.md` already document this distinction).
- Furigana notation: `kanji[reading]` exactly as used in `content/grammar/**/*.md` and parsed by `src/core/ruby.ts`'s `parseRuby` — ruby base is only the immediately-preceding kanji chain; kana between words breaks the chain; a literal space is inserted between bunsetsu (word+attached-particle) groups, matching the existing convention visible in `content/grammar/n5/de-particle.md`'s examples (e.g. `学校[がっこう]で 数学[すうがく]を 勉強[べんきょう]します。`).
- Each paragraph of a text's `## Текст` and `## Перевод` sections must be written as a **single physical line** in the source `.md` file (no manual line-wrapping mid-paragraph); a blank line separates paragraphs. This keeps the parsed `bodyRuby`/`translationRu` strings splittable on `"\n\n"` with no stray bare `"\n"` inside a paragraph.
- Comprehension questions are written and answered **in Russian** (both prompt and choices) — this is a reading-comprehension check on the Japanese narrative's meaning, not a Japanese-question-parsing exercise; the app's grammar/kanji/vocab quiz prompts are likewise already Russian (`QuestionBase.prompt`), so this is consistent with existing convention. **Ruling** (not specified explicitly in the design spec; decided here per "rulings, not stalls").
- Exactly one `[x]` (correct choice) per question; each question has 3 or 4 choices total; each text has 3 or 4 questions. Enforced both at parse time (structural, per-file) and by a `validateTexts` collection-level check, mirroring `validateGrammar`'s split between `parseGrammarFile`'s structural throws and `validateGrammar`'s cross-cutting checks.
- **Ruling:** `TextsListScreen` has **no search box** — content volume is 4-6 items across 2 available levels (vs. hundreds for kanji/vocab), so level tabs alone are sufficient; a search box would be over-engineering for v1. This deliberately diverges from `VocabListScreen`/`KanjiListScreen`'s search bar.
- Texts are **not** part of the SRS/daily-review system — no FSRS cards, no `ItemType` addition, no `session.ts`/`scheduler.ts`/`ReviewScreen.tsx` changes. This is a standalone browse-and-read feature, like `ProgressScreen`.
- No tap-word dictionary popup in v1 (needs Japanese word segmentation, which does not exist anywhere in this codebase — explicitly out of scope, do not build it).
- Node is not on PATH in this environment — every `npm`/`npx` command needs the prefix `export PATH="$PATH:/c/Program Files/nodejs" &&` (Bash).
- **Pre-flight hook-dependency check (done during planning, results below — do not re-derive):** `TextsListScreen` and `TextDetailScreen` are both brand-new files, so introducing `useUserDb()` in them cannot break any *existing* test (there is no existing test that renders a not-yet-created component). The only pre-existing files affected are `src/ui/components/Nav.tsx` (new destination, no new hook) and `tests/ui/Nav.test.tsx` (assertion updated in Task 4). Confirmed via `Grep` over `tests/ui/*.tsx` for `useUserDb|useContentDb` — the only screens without a `useUserDb`/`useContentDb` mock today are ones this plan does not touch (`VocabListScreen.test.tsx`, `VocabDetailScreen.test.tsx`, `KanjiListScreen.test.tsx`, `KanjiDetailScreen.test.tsx`), so no blast radius there.
- **Pre-flight Router-wrapping check:** both new screens' test files are written from scratch in this plan already wrapped in `MemoryRouter` (`TextDetailScreen` additionally in `Routes`/`Route` for `useParams`), so this is a non-issue for this plan — noted only because it was a real gotcha in prior plans this session.
- **Pre-flight baseline (measured, not estimated):** `export PATH="$PATH:/c/Program Files/nodejs" && npx vitest run` on `master` at plan-writing time: **39 test files, 258 tests, all passing.** `Grep` over `tests/e2e/*.ts` for `test(` calls: **23 e2e tests** across 16 files. Every task below states the exact new test count it adds; the final task re-measures both totals and reports the true final numbers rather than asserting a precomputed one.
- Final regression (last task) **must** run `npm run typecheck && npm run lint && npx vitest run && npm run build:desktop && npx playwright test` — not just vitest+playwright — per this session's established lesson that typecheck/lint-only failures have previously surfaced solely at final review.

---

## File Structure

```
content/
  texts/
    n5/
      kitsune-to-tsuru.md       # NEW — n5-kitsune-to-tsuru
      nihiki-no-kaeru.md        # NEW — n5-nihiki-no-kaeru
      watashi-no-chichi.md      # NEW — n5-watashi-no-chichi
      zaru-soba.md               # NEW — n5-zaru-soba
    n4/
      kin-no-ono-gin-no-ono.md  # NEW — n4-kin-no-ono-gin-no-ono
      kyoto-osaka-kaeru.md      # NEW — n4-kyoto-osaka-kaeru
  CREDITS.md                     # MODIFIED — new "Тексты" attribution section

scripts/build-content/
  parse-texts.ts                 # NEW — parseTextFile/loadAllTexts/validateTexts, mirrors parse-grammar.ts
  schema.sql                     # MODIFIED — + texts, text_questions tables
  write-db.ts                    # MODIFIED — + textsDir opt, load/validate/insert texts+questions
  index.ts                       # MODIFIED — + textsDir wired to content/texts

src/core/
  types.ts                       # MODIFIED — + TextQuestion, TextPoint

src/storage/
  content-db.ts                  # MODIFIED — + listTexts, getText

src/ui/
  routes.tsx                     # MODIFIED — + /texts, /texts/:id
  theme.css                      # MODIFIED — + .text-list*, .text-detail*, .text-question* rules
  components/
    Nav.tsx                      # MODIFIED — + "Тексты" destination
  screens/
    TextsListScreen.tsx          # NEW — mirrors VocabListScreen (no search)
    TextDetailScreen.tsx         # NEW — mirrors VocabDetailScreen + own comprehension-question UI

tests/
  fixtures/
    text-valid.md                 # NEW
    text-missing-section.md       # NEW
  scripts/
    parse-texts.test.ts           # NEW
    build-content.test.ts         # MODIFIED — + texts/text_questions table + count assertions
  storage/
    content-db.test.ts            # MODIFIED — + listTexts/getText assertions
  ui/
    TextsListScreen.test.tsx      # NEW
    TextDetailScreen.test.tsx     # NEW
    Nav.test.tsx                  # MODIFIED — 5 -> 6 destinations
  e2e/
    texts-browse.spec.ts          # NEW
```

---

### Task 1: Types, schema, and the `parse-texts.ts` content parser

**Files:**
- Create: `scripts/build-content/parse-texts.ts`
- Modify: `src/core/types.ts` (append `TextQuestion`, `TextPoint`)
- Modify: `scripts/build-content/schema.sql` (append `texts`, `text_questions` tables)
- Create: `tests/fixtures/text-valid.md`
- Create: `tests/fixtures/text-missing-section.md`
- Test: `tests/scripts/parse-texts.test.ts`

**Interfaces:**
- Consumes: `parseRuby` from `src/core/ruby.ts` (`(input: string) => RubySegment[]`, throws on malformed `[`/`]`).
- Produces (used by Task 2's `write-db.ts` and Task 3's `content-db.ts`):
  - `interface TextQuestion { prompt: string; choices: string[]; answerIndex: number }`
  - `interface TextPoint { id: string; level: LevelCode; title: string; bodyRuby: string; translationRu: string; questions: TextQuestion[] }`
  - `parseTextFile(path: string): TextPoint` — throws with a message containing the path and the missing/malformed element.
  - `loadAllTexts(dir: string): TextPoint[]` — one subdirectory per level (e.g. `n5/`, `n4/`), returns `[]` for a missing root dir.
  - `validateTexts(points: TextPoint[]): string[]` — returns human-readable error strings, `[]` if clean.
  - DB tables `texts(id, level, title, body_ruby, translation_ru)` and `text_questions(text_id, ord, prompt, choices_json, answer_index)`.

- [ ] **Step 1: Add `TextQuestion`/`TextPoint` to `src/core/types.ts`**

Append to the end of `src/core/types.ts` (after the existing `VocabPoint` interface):

```typescript
export interface TextQuestion {
  /** Текст вопроса на русском. */
  prompt: string;
  /** 3-4 варианта ответа на русском. */
  choices: string[];
  /** Индекс верного варианта в `choices`. */
  answerIndex: number;
}

export interface TextPoint {
  id: string;
  level: LevelCode;
  title: string;
  /** Японский текст в записи фуриганы ("кандзи[чтение]"), абзацы разделены "\n\n". */
  bodyRuby: string;
  /** Русский перевод, абзацы разделены "\n\n" (столько же абзацев, сколько в bodyRuby). */
  translationRu: string;
  questions: TextQuestion[];
}
```

- [ ] **Step 2: Add `texts`/`text_questions` tables to `scripts/build-content/schema.sql`**

Append to the end of `scripts/build-content/schema.sql` (after the existing `vocab_points`/`idx_vocab_level`, before the `meta` table):

```sql
CREATE TABLE texts (
  id             TEXT PRIMARY KEY,
  level          TEXT NOT NULL REFERENCES levels(code),
  title          TEXT NOT NULL,
  body_ruby      TEXT NOT NULL,
  translation_ru TEXT NOT NULL
);
CREATE INDEX idx_texts_level ON texts(level);

CREATE TABLE text_questions (
  text_id      TEXT NOT NULL REFERENCES texts(id),
  ord          INTEGER NOT NULL,
  prompt       TEXT NOT NULL,
  choices_json TEXT NOT NULL,
  answer_index INTEGER NOT NULL,
  PRIMARY KEY (text_id, ord)
);
```

(The `meta` table stays last in the file — just insert the block above it.)

- [ ] **Step 3: Write the two Markdown fixtures**

Create `tests/fixtures/text-valid.md`:

```markdown
---
id: fix-text-valid
level: N5
title: "тестовый валидный текст"
---

## Текст

これは テスト用[よう]の 文[ぶん]です。とても 短[みじか]い 話[はなし]です。

## Перевод

Это тестовое предложение. Это очень короткая история.

## Вопросы

### Вопрос 1
Это тестовый вопрос?
- [ ] Нет
- [x] Да
- [ ] Может быть

### Вопрос 2
Сколько предложений в тексте?
- [x] Два
- [ ] Одно
- [ ] Три

### Вопрос 3
Это длинная история?
- [ ] Да, очень длинная
- [x] Нет, очень короткая
- [ ] История про кота
```

Create `tests/fixtures/text-missing-section.md`:

```markdown
---
id: fix-text-broken
level: N5
title: "тестовый текст без вопросов"
---

## Текст

これは テスト用[よう]の 文[ぶん]です。

## Перевод

Это тестовое предложение.
```

- [ ] **Step 4: Write the failing test file**

Create `tests/scripts/parse-texts.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { mkdtempSync, writeFileSync, mkdirSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join, resolve } from 'node:path';
import { parseTextFile, loadAllTexts, validateTexts } from '../../scripts/build-content/parse-texts';
import type { TextPoint } from '../../src/core/types';

const fix = (n: string) => resolve(__dirname, '../fixtures', n);

function tmpFile(name: string, content: string): string {
  const dir = mkdtempSync(join(tmpdir(), 'text-test-'));
  const path = join(dir, name);
  writeFileSync(path, content, 'utf8');
  return path;
}

describe('parseTextFile', () => {
  it('parses frontmatter, body, translation and questions', () => {
    const p = parseTextFile(fix('text-valid.md'));
    expect(p.id).toBe('fix-text-valid');
    expect(p.level).toBe('N5');
    expect(p.title).toBe('тестовый валидный текст');
    expect(p.bodyRuby).toContain('テスト');
    expect(p.translationRu).toContain('тестовое предложение');
    expect(p.questions).toHaveLength(3);
    expect(p.questions[0]!.prompt).toBe('Это тестовый вопрос?');
    expect(p.questions[0]!.choices).toEqual(['Нет', 'Да', 'Может быть']);
    expect(p.questions[0]!.answerIndex).toBe(1);
    expect(p.questions[1]!.answerIndex).toBe(0);
  });

  it('throws when a required section is missing', () => {
    expect(() => parseTextFile(fix('text-missing-section.md'))).toThrow(/Вопросы/);
  });

  it('throws when the "## Текст" section is empty', () => {
    const path = tmpFile('bad.md', [
      '---', 'id: t1', 'level: N5', 'title: "t"', '---', '',
      '## Текст', '',
      '## Перевод', '', 'Тест.', '',
      '## Вопросы', '',
      '### Вопрос 1', 'В?', '- [x] Да', '- [ ] Нет', '- [ ] Может быть', '',
    ].join('\n'));
    expect(() => parseTextFile(path)).toThrow(/empty "## Текст" section/);
  });

  it('throws when a question has fewer than 3 choices', () => {
    const path = tmpFile('bad.md', [
      '---', 'id: t1', 'level: N5', 'title: "t"', '---', '',
      '## Текст', '', 'テスト。', '',
      '## Перевод', '', 'Тест.', '',
      '## Вопросы', '',
      '### Вопрос 1', 'Вопрос?', '- [x] A', '- [ ] B', '',
    ].join('\n'));
    expect(() => parseTextFile(path)).toThrow(/2 choices \(need 3-4\)/);
  });

  it('throws when a question has more than 4 choices', () => {
    const path = tmpFile('bad.md', [
      '---', 'id: t1', 'level: N5', 'title: "t"', '---', '',
      '## Текст', '', 'テスト。', '',
      '## Перевод', '', 'Тест.', '',
      '## Вопросы', '',
      '### Вопрос 1', 'Вопрос?',
      '- [x] A', '- [ ] B', '- [ ] C', '- [ ] D', '- [ ] E', '',
    ].join('\n'));
    expect(() => parseTextFile(path)).toThrow(/5 choices \(need 3-4\)/);
  });

  it('throws when no choice is marked correct', () => {
    const path = tmpFile('bad.md', [
      '---', 'id: t1', 'level: N5', 'title: "t"', '---', '',
      '## Текст', '', 'テスト。', '',
      '## Перевод', '', 'Тест.', '',
      '## Вопросы', '',
      '### Вопрос 1', 'Вопрос?', '- [ ] A', '- [ ] B', '- [ ] C', '',
    ].join('\n'));
    expect(() => parseTextFile(path)).toThrow(/0 correct answers marked \(need exactly 1\)/);
  });

  it('throws when more than one choice is marked correct', () => {
    const path = tmpFile('bad.md', [
      '---', 'id: t1', 'level: N5', 'title: "t"', '---', '',
      '## Текст', '', 'テスト。', '',
      '## Перевод', '', 'Тест.', '',
      '## Вопросы', '',
      '### Вопрос 1', 'Вопрос?', '- [x] A', '- [x] B', '- [ ] C', '',
    ].join('\n'));
    expect(() => parseTextFile(path)).toThrow(/2 correct answers marked \(need exactly 1\)/);
  });

  it('throws when a question heading has no prompt text', () => {
    const path = tmpFile('bad.md', [
      '---', 'id: t1', 'level: N5', 'title: "t"', '---', '',
      '## Текст', '', 'テスト。', '',
      '## Перевод', '', 'Тест.', '',
      '## Вопросы', '',
      '### Вопрос 1', '- [x] A', '- [ ] B', '- [ ] C', '',
    ].join('\n'));
    expect(() => parseTextFile(path)).toThrow(/no prompt text/);
  });
});

describe('loadAllTexts', () => {
  it('derives the level from the directory name and merges every .md file in it', () => {
    const dir = mkdtempSync(join(tmpdir(), 'texts-dir-'));
    const n5Dir = join(dir, 'n5');
    const n4Dir = join(dir, 'n4');
    mkdirSync(n5Dir);
    mkdirSync(n4Dir);
    writeFileSync(join(n5Dir, 'a.md'), [
      '---', 'id: n5-a', 'level: N5', 'title: "a"', '---', '',
      '## Текст', '', 'あ。', '',
      '## Перевод', '', 'А.', '',
      '## Вопросы', '',
      '### Вопрос 1', 'В?', '- [x] Да', '- [ ] Нет', '- [ ] Может быть', '',
    ].join('\n'), 'utf8');
    writeFileSync(join(n4Dir, 'b.md'), [
      '---', 'id: n4-b', 'level: N4', 'title: "b"', '---', '',
      '## Текст', '', 'い。', '',
      '## Перевод', '', 'Б.', '',
      '## Вопросы', '',
      '### Вопрос 1', 'В?', '- [x] Да', '- [ ] Нет', '- [ ] Может быть', '',
    ].join('\n'), 'utf8');
    const points = loadAllTexts(dir);
    expect(points.map((p) => p.id).sort()).toEqual(['n4-b', 'n5-a']);
    expect(points.find((p) => p.id === 'n5-a')!.level).toBe('N5');
  });

  it('returns an empty array for a missing directory instead of throwing', () => {
    expect(loadAllTexts('/no/such/dir/at/all')).toEqual([]);
  });
});

describe('validateTexts', () => {
  const q = (correct = 0): TextPoint['questions'][number] => ({
    prompt: 'В?', choices: ['A', 'B', 'C'], answerIndex: correct,
  });
  const base = (over: Partial<TextPoint> = {}): TextPoint => ({
    id: 't1', level: 'N5', title: 't', bodyRuby: 'テスト[よう]です。',
    translationRu: 'тест', questions: [q(), q(), q()],
    ...over,
  });

  it('passes clean data', () => {
    expect(validateTexts([base()])).toEqual([]);
  });

  it('flags a duplicate id', () => {
    expect(validateTexts([base(), base()]).some((e) => e.includes('duplicate'))).toBe(true);
  });

  it('flags too few questions', () => {
    const errors = validateTexts([base({ questions: [q(), q()] })]);
    expect(errors.some((e) => e.includes('need 3-4'))).toBe(true);
  });

  it('flags too many questions', () => {
    const errors = validateTexts([base({ questions: [q(), q(), q(), q(), q()] })]);
    expect(errors.some((e) => e.includes('need 3-4'))).toBe(true);
  });

  it('flags an out-of-range answerIndex', () => {
    const errors = validateTexts([base({ questions: [q(9), q(), q()] })]);
    expect(errors.some((e) => e.includes('out of range'))).toBe(true);
  });

  it('flags a ruby base that contains kana', () => {
    const errors = validateTexts([base({ bodyRuby: 'おちゃ[ちゃ]です。' })]);
    expect(errors.some((e) => /ruby base .* contains kana/.test(e))).toBe(true);
  });

  it('flags unparseable ruby', () => {
    const errors = validateTexts([base({ bodyRuby: 'これは[test' })]);
    expect(errors.some((e) => /unparseable ruby/.test(e))).toBe(true);
  });
});
```

- [ ] **Step 5: Run the test to verify it fails**

Run: `export PATH="$PATH:/c/Program Files/nodejs" && npx vitest run tests/scripts/parse-texts.test.ts`
Expected: FAIL — `Cannot find module '../../scripts/build-content/parse-texts'`.

- [ ] **Step 6: Implement `scripts/build-content/parse-texts.ts`**

```typescript
import { readFileSync, readdirSync } from 'node:fs';
import { join } from 'node:path';
import matter from 'gray-matter';
import type { TextPoint, TextQuestion } from '../../src/core/types';
import { parseRuby } from '../../src/core/ruby';

/** Хирагана + катакана (вкл. полуширинную) — не должны попадать в базу чтения "кандзи[чтение]". */
const KANA = /[぀-ヿｦ-ﾟ]/;

const REQUIRED_SECTIONS = ['## Текст', '## Перевод', '## Вопросы'];

export function parseTextFile(path: string): TextPoint {
  const raw = readFileSync(path, 'utf8');
  const { data, content } = matter(raw);

  for (const key of ['id', 'level', 'title'] as const) {
    if (data[key] === undefined) throw new Error(`${path}: frontmatter missing "${key}"`);
  }
  for (const section of REQUIRED_SECTIONS) {
    if (!content.includes(section)) {
      throw new Error(`${path}: missing section "${section}"`);
    }
  }

  return {
    id: String(data['id']),
    level: String(data['level']),
    title: String(data['title']),
    bodyRuby: extractSection(content, '## Текст', ['## Перевод'], path),
    translationRu: extractSection(content, '## Перевод', ['## Вопросы'], path),
    questions: parseQuestions(content, path),
  };
}

function extractSection(content: string, heading: string, stopHeadings: string[], path: string): string {
  const lines = content.split('\n');
  const start = lines.findIndex((l) => l.trim() === heading);
  if (start === -1) throw new Error(`${path}: no "${heading}" section`);
  let end = lines.length;
  for (let i = start + 1; i < lines.length; i++) {
    if (stopHeadings.includes(lines[i]!.trim())) {
      end = i;
      break;
    }
  }
  const body = lines.slice(start + 1, end).join('\n').trim();
  if (!body) throw new Error(`${path}: empty "${heading}" section`);
  return body;
}

function parseQuestions(content: string, path: string): TextQuestion[] {
  const lines = content.split('\n');
  const qStart = lines.findIndex((l) => l.trim() === '## Вопросы');
  if (qStart === -1) throw new Error(`${path}: no "## Вопросы" section`);
  const section = lines.slice(qStart + 1);

  const headingIdxs: number[] = [];
  section.forEach((l, i) => {
    if (l.trim().startsWith('### ')) headingIdxs.push(i);
  });
  if (headingIdxs.length === 0) {
    throw new Error(`${path}: "## Вопросы" section has no "### Вопрос N" headings`);
  }

  const out: TextQuestion[] = [];
  for (let h = 0; h < headingIdxs.length; h++) {
    const blockStart = headingIdxs[h]! + 1;
    const blockEnd = h + 1 < headingIdxs.length ? headingIdxs[h + 1]! : section.length;
    const block = section
      .slice(blockStart, blockEnd)
      .map((l) => l.trim())
      .filter((l) => l !== '');

    const promptLine = block.find((l) => !l.startsWith('- ['));
    if (!promptLine) throw new Error(`${path}: question ${h + 1} has no prompt text`);

    const choiceLines = block.filter((l) => l.startsWith('- ['));
    if (choiceLines.length < 3 || choiceLines.length > 4) {
      throw new Error(`${path}: question ${h + 1} has ${choiceLines.length} choices (need 3-4)`);
    }

    const checkedIdxs: number[] = [];
    choiceLines.forEach((l, i) => {
      if (l.startsWith('- [x]')) checkedIdxs.push(i);
    });
    if (checkedIdxs.length !== 1) {
      throw new Error(
        `${path}: question ${h + 1} has ${checkedIdxs.length} correct answers marked (need exactly 1)`,
      );
    }

    out.push({
      prompt: promptLine,
      choices: choiceLines.map((l) => l.replace(/^- \[[x ]\]\s*/, '')),
      answerIndex: checkedIdxs[0]!,
    });
  }
  return out;
}

export function loadAllTexts(dir: string): TextPoint[] {
  let levelDirs: string[];
  try {
    levelDirs = readdirSync(dir);
  } catch {
    return [];
  }
  const out: TextPoint[] = [];
  for (const level of levelDirs) {
    const levelDir = join(dir, level);
    let files: string[];
    try {
      files = readdirSync(levelDir).filter((f) => f.endsWith('.md'));
    } catch {
      continue;
    }
    for (const f of files) out.push(parseTextFile(join(levelDir, f)));
  }
  return out;
}

export function validateTexts(points: TextPoint[]): string[] {
  const errors: string[] = [];
  const ids = new Set<string>();
  for (const p of points) {
    if (ids.has(p.id)) errors.push(`duplicate text id "${p.id}"`);
    ids.add(p.id);

    if (p.questions.length < 3 || p.questions.length > 4) {
      errors.push(`${p.id}: ${p.questions.length} questions (need 3-4)`);
    }
    for (const q of p.questions) {
      if (q.answerIndex < 0 || q.answerIndex >= q.choices.length) {
        errors.push(`${p.id}: answerIndex ${q.answerIndex} out of range for question "${q.prompt}"`);
      }
    }

    let segs;
    try {
      segs = parseRuby(p.bodyRuby);
    } catch (err) {
      errors.push(`${p.id}: unparseable ruby in text body: ${(err as Error).message}`);
      continue;
    }
    for (const s of segs) {
      if (s.ruby !== null && KANA.test(s.base)) {
        errors.push(`${p.id}: ruby base "${s.base}" contains kana in text body`);
      }
    }
  }
  return errors;
}
```

- [ ] **Step 7: Run the test to verify it passes**

Run: `export PATH="$PATH:/c/Program Files/nodejs" && npx vitest run tests/scripts/parse-texts.test.ts`
Expected: PASS — 16 tests (3 `parseTextFile` structural throws not counted individually — recount: 2 happy/missing-section + 6 malformed-question throws = 8 in `parseTextFile`, 2 in `loadAllTexts`, 7 in `validateTexts` = 17 tests total in this file). Confirm the actual number in the terminal output rather than trusting this parenthetical.

- [ ] **Step 8: Run the full suite and typecheck to confirm no regressions**

Run: `export PATH="$PATH:/c/Program Files/nodejs" && npx tsc -p tsconfig.json --noEmit && npx vitest run`
Expected: typecheck clean; all previous 258 tests plus this file's new tests pass.

- [ ] **Step 9: Commit**

```bash
git add src/core/types.ts scripts/build-content/schema.sql scripts/build-content/parse-texts.ts tests/fixtures/text-valid.md tests/fixtures/text-missing-section.md tests/scripts/parse-texts.test.ts
git commit -m "feat(texts): add TextPoint/TextQuestion types, schema tables, and parse-texts.ts"
```

---

### Task 2: Real content — 6 authored texts, `content/CREDITS.md`, and pipeline wiring

**Files:**
- Create: `content/texts/n5/kitsune-to-tsuru.md`
- Create: `content/texts/n5/nihiki-no-kaeru.md`
- Create: `content/texts/n5/watashi-no-chichi.md`
- Create: `content/texts/n5/zaru-soba.md`
- Create: `content/texts/n4/kin-no-ono-gin-no-ono.md`
- Create: `content/texts/n4/kyoto-osaka-kaeru.md`
- Modify: `content/CREDITS.md`
- Modify: `scripts/build-content/write-db.ts`
- Modify: `scripts/build-content/index.ts`
- Test: `tests/scripts/build-content.test.ts`

**Interfaces:**
- Consumes: `loadAllTexts`, `validateTexts`, `TextPoint`/`TextQuestion` from Task 1.
- Produces: populated `texts`/`text_questions` tables in the built `content.db`, consumed by Task 3's `ContentDb.listTexts`/`getText`.

**Content-authoring note (why these 6 texts, not Tadoku's literal text):** During planning, `WebFetch` was used against `tadoku.org`'s free-books listing and an individual book page (`https://tadoku.org/japanese/book/2335/`). The listing confirmed the Level 0 / Level 1 title lists named in the design spec (キツネとツル, ざるそば, 2匹のカエル, ぼくのお父さん for Level 0; 金の斧銀の斧, 京都のかえると大阪のかえる for Level 1), but every book's actual page is a purchase/catalog page — "no readable Japanese narrative text visible on the page itself" per the fetch result; access is via a `FREE` icon leading to a PDF download or a YouTube read-along, neither of which is a webpage this tool can extract prose from. Per the design brief this is an expected, acceptable outcome: **all 6 texts below are original prose written for this app**, using only the corresponding Tadoku title as a name/theme, or — for the two texts based on genuinely famous public-domain tales (Aesop's fox-and-crane / honest-woodcutter fables, and the traditional Japanese "Kyoto frog and Osaka frog" folktale) — a public-domain plot retold in the author's own words. This is documented precisely, per-text, in the `content/CREDITS.md` addition below.

- [ ] **Step 1: Write the 4 N5 text files**

Create `content/texts/n5/kitsune-to-tsuru.md`:

```markdown
---
id: n5-kitsune-to-tsuru
level: N5
title: "キツネとツル"
---

## Текст

むかし、きつねと つるが いました。きつねと つるは 友達[ともだち]でした。

ある 日[ひ]、きつねは つるを 晩[ばん]ご飯[はん]に 呼[よ]びました。きつねは 平[ひら]たい お皿[さら]に スープを 入[い]れました。きつねは スープを すぐに 飲[の]みました。でも、つるの くちばしは 長[なが]いですから、つるは スープを 飲[の]むことが できませんでした。つるは 何[なに]も 食[た]べないで 家[いえ]に 帰[かえ]りました。

つぎの 日[ひ]、つるは きつねを 晩[ばん]ご飯[はん]に 呼[よ]びました。つるは 長[なが]くて 細[ほそ]い 瓶[びん]に スープを 入[い]れました。つるは くちばしで スープを 上手[じょうず]に 飲[の]みました。でも、きつねの 口[くち]は 瓶[びん]に 入[はい]りませんでしたから、きつねは スープを 飲[の]むことが できませんでした。きつねも 何[なに]も 食[た]べないで 家[いえ]に 帰[かえ]りました。

## Перевод

Давным-давно жили лиса и журавль. Лиса и журавль были друзьями.

Однажды лиса пригласила журавля на ужин. Лиса налила суп в плоскую тарелку. Лиса сразу же выпила свой суп. Но клюв журавля был длинным, и журавль не смог выпить суп. Журавль ушёл домой голодным, ничего не поев.

На следующий день журавль пригласил лису на ужин. Журавль налил суп в узкий высокий кувшин. Журавль ловко выпил суп клювом. Но рот лисы не помещался в кувшин, и лиса не смогла выпить суп. Лиса тоже ушла домой голодной, ничего не поев.

## Вопросы

### Вопрос 1
Во что лиса налила суп в первый раз?
- [x] В плоскую тарелку
- [ ] В высокий стакан
- [ ] В глубокую миску
- [ ] В коробку

### Вопрос 2
Почему журавль не смог поесть суп у лисы?
- [x] У него был длинный клюв
- [ ] Суп был слишком горячим
- [ ] Он не любит суп
- [ ] Он не был голоден

### Вопрос 3
Во что журавль налил суп на следующий день?
- [x] В узкий высокий кувшин
- [ ] В плоскую тарелку
- [ ] В большую чашку
- [ ] В котёл

### Вопрос 4
Чем закончилась история?
- [x] Лиса тоже не смогла поесть суп
- [ ] Они помирились и поели вместе
- [ ] Лиса извинилась перед журавлём
- [ ] Журавль улетел навсегда
```

Create `content/texts/n5/nihiki-no-kaeru.md`:

```markdown
---
id: n5-nihiki-no-kaeru
level: N5
title: "二匹のかえる"
---

## Текст

二匹[にひき]の かえるが いました。一匹[いっぴき]は 井戸[いど]の 中[なか]に 住[す]んでいました。もう 一匹[いっぴき]は 海[うみ]の 近[ちか]くに 住[す]んでいました。

ある 日[ひ]、海[うみ]の かえるは 井戸[いど]に 遊[あそ]びに 来[き]ました。井戸[いど]の かえるは「この 井戸[いど]は とても 広[ひろ]いです。私[わたし]の 家[いえ]は 一番[いちばん] いい 家[いえ]です」と 言[い]いました。

海[うみ]の かえるは 笑[わら]って、「井戸[いど]も いいですが、海[うみ]は もっと 大[おお]きいですよ。海[うみ]には 終[お]わりが ありません」と 言[い]いました。

井戸[いど]の かえるは びっくりしました。今[いま]まで、井戸[いど]より 大[おお]きい 場所[ばしょ]が あることを 知[し]りませんでした。次[つぎ]の 日[ひ]、井戸[いど]の かえるは 海[うみ]を 見[み]に 行[い]きました。海[うみ]は 本当[ほんとう]に とても 大[おお]きかったです。

## Перевод

Жили-были две лягушки. Одна жила в колодце. Другая жила недалеко от моря.

Однажды лягушка с моря пришла в гости в колодец. Лягушка из колодца сказала: «Этот колодец очень просторный. Мой дом — самый лучший дом на свете».

Морская лягушка засмеялась и сказала: «Колодец, конечно, хорош, но море гораздо больше. У моря вообще нет конца».

Лягушка из колодца очень удивилась. До этого она не знала, что бывают места больше колодца. На следующий день лягушка из колодца пошла посмотреть на море. Море оказалось действительно очень большим.

## Вопросы

### Вопрос 1
Где жила первая лягушка?
- [x] В колодце
- [ ] В море
- [ ] В реке
- [ ] В лесу

### Вопрос 2
Что сказала лягушка с моря?
- [x] Море гораздо больше колодца, и у него нет конца
- [ ] У колодца нет дна
- [ ] Она никогда не видела колодца
- [ ] Она боится моря

### Вопрос 3
Почему лягушка из колодца удивилась?
- [x] Она не знала, что есть место больше колодца
- [ ] Она никогда раньше не видела лягушек
- [ ] Колодец оказался пустым
- [ ] Она не умела плавать

### Вопрос 4
Что лягушка из колодца сделала после этого разговора?
- [x] Пошла посмотреть на море
- [ ] Осталась в колодце навсегда
- [ ] Переехала жить в море
- [ ] Поссорилась с другой лягушкой
```

Create `content/texts/n5/watashi-no-chichi.md`:

```markdown
---
id: n5-watashi-no-chichi
level: N5
title: "私の父"
---

## Текст

私[わたし]の 父[ちち]は 会社員[かいしゃいん]です。毎朝[まいあさ] 七時[しちじ]に 起[お]きて、電車[でんしゃ]で 会社[かいしゃ]へ 行[い]きます。父[ちち]の 会社[かいしゃ]は 駅[えき]から 近[ちか]いです。

父[ちち]は 料理[りょうり]が 好[す]きです。日曜日[にちようび]に、よく 家族[かぞく]の ために カレーを 作[つく]ります。父[ちち]の カレーは とても おいしいです。

父[ちち]は スポーツも 好[す]きです。週末[しゅうまつ]に、公園[こうえん]で テニスを します。私[わたし]も ときどき 一緒[いっしょ]に テニスを します。

私[わたし]は 父[ちち]が 大好[だいす]きです。

## Перевод

Мой отец — служащий компании. Каждое утро он встаёт в семь часов и едет на работу на электричке. Компания отца находится недалеко от станции.

Отец любит готовить. По воскресеньям он часто готовит карри для всей семьи. Карри у отца очень вкусное.

Отец также любит спорт. По выходным он играет в теннис в парке. Я тоже иногда играю с ним в теннис.

Я очень люблю своего отца.

## Вопросы

### Вопрос 1
Кем работает отец?
- [x] Служащим компании
- [ ] Учителем
- [ ] Поваром
- [ ] Врачом

### Вопрос 2
Что отец делает по воскресеньям?
- [x] Готовит карри для семьи
- [ ] Играет в теннис
- [ ] Идёт на работу
- [ ] Спит весь день

### Вопрос 3
Где отец играет в теннис?
- [x] В парке
- [ ] Дома
- [ ] На станции
- [ ] В своей компании

### Вопрос 4
Как автор относится к отцу?
- [x] Очень его любит
- [ ] Немного его боится
- [ ] Почти с ним не видится
- [ ] Считает его слишком строгим
```

Create `content/texts/n5/zaru-soba.md`:

```markdown
---
id: n5-zaru-soba
level: N5
title: "ざるそば"
---

## Текст

今日[きょう]は とても 暑[あつ]い 日[ひ]です。田中[たなか]さんは 昼[ひる]ご飯[はん]に 何[なに]を 食[た]べるか 考[かんが]えました。

暑[あつ]い 日[ひ]には、冷[つめ]たい 食[た]べ物[もの]が いいです。田中[たなか]さんは 近[ちか]くの そば屋[や]に 入[はい]りました。そして、ざるそばを 注文[ちゅうもん]しました。

少[すこ]し 待[ま]つと、冷[つめ]たい そばが 来[き]ました。田中[たなか]さんは そばを つゆに つけて 食[た]べました。とても おいしかったです。

食[た]べた 後[あと]、田中[たなか]さんは 少[すこ]し 元気[げんき]に なりました。そして、また 仕事[しごと]に 戻[もど]りました。

## Перевод

Сегодня очень жаркий день. Танака-сан думал, что бы съесть на обед.

В жаркий день хороша холодная еда. Танака-сан зашёл в соседнюю лапшичную. Там он заказал дзару-соба (холодную гречневую лапшу).

Через некоторое время принесли холодную лапшу. Танака-сан ел лапшу, макая её в соус цую. Было очень вкусно.

Поев, Танака-сан немного взбодрился. И снова вернулся к работе.

## Вопросы

### Вопрос 1
Какая сегодня погода?
- [x] Жарко
- [ ] Холодно
- [ ] Идёт дождь
- [ ] Идёт снег

### Вопрос 2
Что съел Танака-сан?
- [x] Холодную гречневую лапшу дзару-соба
- [ ] Горячий рамен
- [ ] Карри
- [ ] Бутерброд

### Вопрос 3
Как Танака-сан ел лапшу?
- [x] Макая её в соус цую
- [ ] Без всякого соуса
- [ ] С супом мисо
- [ ] С рисом

### Вопрос 4
Что случилось с Танакой-сан после еды?
- [x] Он немного взбодрился
- [ ] Он заснул
- [ ] Он заболел
- [ ] Он остался голодным
```

- [ ] **Step 2: Write the 2 N4 text files**

Create `content/texts/n4/kin-no-ono-gin-no-ono.md`:

```markdown
---
id: n4-kin-no-ono-gin-no-ono
level: N4
title: "金の斧銀の斧"
---

## Текст

昔[むかし]、正直[しょうじき]な 木[き]こりが いました。ある 日[ひ]、木[き]こりは 森[もり]で 木[き]を 切[き]っていました。そのとき、大切[たいせつ]な 斧[おの]を 誤[あやま]って 池[いけ]に 落[お]としてしまいました。

木[き]こりが 困[こま]っていると、池[いけ]の 中[なか]から 神様[かみさま]が 現[あらわ]れました。神様[かみさま]は 金[きん]の 斧[おの]を 持[も]って、「これは あなたの 斧[おの]ですか」と 聞[き]きました。木[き]こりは 「いいえ、それは 私[わたし]の 斧[おの]では ありません」と 答[こた]えました。

次[つぎ]に、神様[かみさま]は 銀[ぎん]の 斧[おの]を 持[も]って 来[き]ました。木[き]こりは また 「それも 私[わたし]の 斧[おの]では ありません」と 言[い]いました。

最後[さいご]に、神様[かみさま]は 木[き]こりの 古[ふる]い 鉄[てつ]の 斧[おの]を 持[も]って 来[き]ました。木[き]こりは 喜[よろこ]んで 「はい、それが 私[わたし]の 斧[おの]です」と 言[い]いました。

神様[かみさま]は 木[き]こりの 正直[しょうじき]さに 感心[かんしん]して、金[きん]の 斧[おの]と 銀[ぎん]の 斧[おの]も 木[き]こりに あげました。

このことを 聞[き]いた 欲[よく]の 深[ふか]い 別[べつ]の 木[き]こりは、わざと 自分[じぶん]の 斧[おの]を 池[いけ]に 落[お]としました。神様[かみさま]が 金[きん]の 斧[おの]を 見[み]せると、その 木[き]こりは うそを ついて 「はい、それは 私[わたし]の 斧[おの]です」と 言[い]いました。神様[かみさま]は 彼[かれ]の うそに 気[き]づいて、金[きん]の 斧[おの]も 彼[かれ]自身[じしん]の 斧[おの]も あげませんでした。

## Перевод

Давным-давно жил честный дровосек. Однажды дровосек рубил дрова в лесу. В этот момент он случайно уронил в пруд свой драгоценный топор.

Пока дровосек горевал, из пруда явился бог. Бог держал золотой топор и спросил: «Это твой топор?» Дровосек ответил: «Нет, это не мой топор».

Затем бог принёс серебряный топор. Дровосек снова сказал: «Это тоже не мой топор».

Наконец бог принёс старый железный топор дровосека. Дровосек обрадовался и сказал: «Да, это мой топор».

Бог, восхищённый честностью дровосека, подарил ему ещё и золотой, и серебряный топоры.

Услышав об этом, другой, жадный дровосек нарочно уронил свой топор в пруд. Когда бог показал ему золотой топор, тот солгал и сказал: «Да, это мой топор». Бог заметил его ложь и не отдал ему ни золотой топор, ни его собственный.

## Вопросы

### Вопрос 1
Почему дровосек расстроился?
- [ ] Он заблудился в лесу
- [x] Он уронил свой топор в пруд
- [ ] Он поранился топором
- [ ] Он забыл дома обед

### Вопрос 2
Какой топор бог показал первым?
- [x] Золотой
- [ ] Серебряный
- [ ] Железный
- [ ] Деревянный

### Вопрос 3
Какой топор дровосек признал своим?
- [ ] Золотой
- [ ] Серебряный
- [x] Старый железный
- [ ] Все три топора сразу

### Вопрос 4
Что случилось со вторым, жадным дровосеком?
- [x] Он не получил ни одного топора, даже своего
- [ ] Он получил все три топора
- [ ] Он получил только серебряный топор
- [ ] Бог наказал его тюрьмой
```

Create `content/texts/n4/kyoto-osaka-kaeru.md`:

```markdown
---
id: n4-kyoto-osaka-kaeru
level: N4
title: "京都のかえると大阪のかえる"
---

## Текст

京都[きょうと]に 住[す]んでいる かえると、大阪[おおさか]に 住[す]んでいる かえるが いました。京都[きょうと]の かえるは 大阪[おおさか]を 見[み]たいと 思[おも]い、大阪[おおさか]の かえるは 京都[きょうと]を 見[み]たいと 思[おも]いました。

ある 日[ひ]、二匹[にひき]は それぞれ 旅[たび]に 出[で]ました。二匹[にひき]は 京都[きょうと]と 大阪[おおさか]の 間[あいだ]に ある 山[やま]の 上[うえ]で 出会[であ]いました。

長[なが]い 道[みち]を 歩[ある]いて、二匹[にひき]とも 疲[つか]れていました。そこで、後[うし]ろ 足[あし]で 立[た]って、遠[とお]くの 町[まち]を 見[み]ることに しました。かえるの 目[め]は 頭[あたま]の 上[うえ]に ありますから、立[た]つと、進[すす]む 方向[ほうこう]では なく、来[き]た 方向[ほうこう]が 見[み]えて しまいます。

京都[きょうと]の かえるは 立[た]って、大阪[おおさか]の ほうを 見[み]た つもりでしたが、実[じつ]は 京都[きょうと]の 町[まち]が 見[み]えていました。大阪[おおさか]の かえるも 同[おな]じように、実[じつ]は 大阪[おおさか]の 町[まち]が 見[み]えていました。

「なんだ、大阪[おおさか]も 京都[きょうと]と 同[おな]じ 町[まち]みたいだ」と 京都[きょうと]の かえるは 言[い]いました。大阪[おおさか]の かえるも「京都[きょうと]も 大阪[おおさか]と 同[おな]じ 町[まち]みたいだ」と 言[い]いました。

二匹[にひき]は がっかりして、これ 以上[いじょう] 歩[ある]く 必要[ひつよう]は ないと 思[おも]い、それぞれ 自分[じぶん]の 町[まち]に 帰[かえ]りました。しかし、二匹[にひき]が 見[み]たのは 本当[ほんとう]は 自分[じぶん]の 町[まち]だったのです。

## Перевод

Жила лягушка, которая жила в Киото, и лягушка, которая жила в Осаке. Лягушка из Киото хотела увидеть Осаку, а лягушка из Осаки хотела увидеть Киото.

Однажды они обе отправились в путь. Они встретились на горе между Киото и Осакой.

Пройдя долгий путь, обе лягушки устали. Тогда они решили встать на задние лапы и посмотреть на далёкий город. Поскольку глаза лягушки находятся на макушке, когда она встаёт, ей видно не то направление, куда она идёт, а то, откуда она пришла.

Лягушка из Киото встала, собираясь посмотреть в сторону Осаки, но на самом деле ей был виден город Киото. Лягушка из Осаки точно так же на самом деле видела город Осаку.

«Надо же, Осака выглядит так же, как Киото», — сказала лягушка из Киото. Лягушка из Осаки тоже сказала: «Киото выглядит так же, как Осака».

Обе лягушки расстроились и решили, что идти дальше незачем, и вернулись каждая в свой город. Но на самом деле то, что они видели, было их собственным городом.

## Вопросы

### Вопрос 1
Что хотела сделать лягушка из Киото?
- [x] Увидеть Осаку
- [ ] Переехать жить в Осаку
- [ ] Победить лягушку из Осаки в состязании
- [ ] Найти себе пару

### Вопрос 2
Где встретились две лягушки?
- [ ] В Киото
- [ ] В Осаке
- [x] На горе между Киото и Осакой
- [ ] У реки

### Вопрос 3
Почему лягушки увидели не тот город, который хотели?
- [ ] Была ночь, и ничего не было видно
- [x] Глаза лягушки находятся сверху головы, и, встав, она видит то, откуда пришла
- [ ] Пошёл сильный дождь
- [ ] Они перепутали дорогу

### Вопрос 4
Чем закончилась история?
- [x] Обе лягушки решили, что города одинаковы, и вернулись домой
- [ ] Лягушки дошли до цели своего путешествия
- [ ] Лягушки поссорились и расстались врагами
- [ ] Лягушки остались жить вместе на горе
```

- [ ] **Step 3: Add the "Тексты" attribution section to `content/CREDITS.md`**

Append to the end of `content/CREDITS.md`:

```markdown
## Тексты — охват (список коротких текстов для чтения)

- **Tadoku / にほんご多読 (NPO多言語多読, tadoku.org)** — https://tadoku.org/japanese/en/free-books-en/
  Лицензия исходных изданий: Creative Commons Attribution-NonCommercial-NoDerivatives (CC BY-NC-ND) —
  только личное некоммерческое использование, без переиздания и без производных публикаций
  (осознанное решение пользователя для собственного, никогда не публикуемого приложения).
  Список названий уровня Level 0 (キツネとツル, ざるそば, 2匹のカエル, ぼくのお父さん и др.) и Level 1
  (金の斧銀の斧, 京都のかえると大阪のかえる и др.) использован только как список тем/названий.
  **Фактический текст изданий Tadoku не был доступен**: сайт распространяет истории как PDF для
  скачивания или как видео на YouTube — ни то, ни другое не является читаемой веб-страницей,
  с которой можно снять текст (проверено прямым запросом к странице конкретного издания:
  https://tadoku.org/japanese/book/2335/ — страница оказалась карточкой товара без текста истории).
  Поэтому весь японский текст, русский перевод и все вопросы на понимание для всех 6 текстов —
  собственные, написанные автором приложения специально для этого раздела:
  - **«キツネとツル» (n5-kitsune-to-tsuru)** и **«金の斧銀の斧» (n4-kin-no-ono-gin-no-ono)** —
    пересказ общеизвестных сюжетов басен Эзопа (лиса и журавль; честный дровосек и золотой/
    серебряный топоры) — сюжеты общественного достояния, текст на японском и русский перевод
    написаны автором заново, не являются переводом текста Tadoku.
  - **«京都のかえると大阪のかえる» (n4-kyoto-osaka-kaeru)** — пересказ традиционной японской
    народной сказки о лягушках из Киото и Осаки (сюжет общественного достояния, известен
    независимо от Tadoku) — текст и перевод написаны автором заново.
  - **«二匹のかえる» (n5-nihiki-no-kaeru)**, **«私の父» (n5-watashi-no-chichi)** и
    **«ざるそば» (n5-zaru-soba)** — не основаны на известном автору сюжете конкретного издания
    Tadoku; это полностью авторские небольшие тексты, вдохновлённые только темой/названием
    из списка Tadoku (два земноводных друга, рассказ о своём отце, обед в жару).
  Уровень сложности (N5 для текстов из Tadoku Level 0, N4 — из Level 1) выбран по аналогии с
  условным соответствием уровней Tadoku и JLPT, а не как официальное сопоставление издателя.
```

- [ ] **Step 4: Wire `textsDir` into `write-db.ts`**

In `scripts/build-content/write-db.ts`, add the import (after the existing `loadAllVocab, validateVocab` import):

```typescript
import { loadAllTexts, validateTexts } from './parse-texts';
```

Add `textsDir` to the `BuildOpts` interface:

```typescript
export interface BuildOpts {
  grammarDir: string;
  levelsYml: string;
  schemaPath: string;
  kanjiDir: string;
  vocabDir: string;
  textsDir: string;
  /** По умолчанию — фиксированное значение, чтобы сборка была детерминированной в тестах. */
  contentVersion?: string;
}
```

In `buildContentDb`, insert the following block right before the existing `db.run("INSERT INTO meta ...` lines at the end of the function (i.e. after the vocab-insertion block that ends with `insV.free();`):

```typescript
  const texts = loadAllTexts(opts.textsDir).sort((a, b) => a.id.localeCompare(b.id));
  const textErrors = validateTexts(texts);
  if (textErrors.length) throw new Error(`texts validation failed:\n${textErrors.join('\n')}`);
  for (const t of texts) {
    if (!levelCodes.has(t.level)) throw new Error(`text ${t.id}: unknown level ${t.level}`);
  }
  const insT = db.prepare(
    'INSERT INTO texts (id, level, title, body_ruby, translation_ru) VALUES (?,?,?,?,?)',
  );
  const insTQ = db.prepare(
    'INSERT INTO text_questions (text_id, ord, prompt, choices_json, answer_index) VALUES (?,?,?,?,?)',
  );
  for (const t of texts) {
    insT.run([t.id, t.level, t.title, t.bodyRuby, t.translationRu]);
    t.questions.forEach((q, i) => insTQ.run([t.id, i, q.prompt, JSON.stringify(q.choices), q.answerIndex]));
  }
  insT.free();
  insTQ.free();
```

(This must land before the existing `db.run("INSERT INTO meta (key, value) VALUES ('content_version', ?)"...)` lines so `texts`/`text_questions` rows exist before `meta` is written — order among tables without FK dependencies on `meta` doesn't otherwise matter, but keeping the new block adjacent to the other content-type blocks matches the file's existing structure.)

- [ ] **Step 5: Wire `content/texts` into `scripts/build-content/index.ts`**

In `scripts/build-content/index.ts`, add `textsDir` to the `buildContentDb` call:

```typescript
  const bytes = buildContentDb({
    grammarDir: resolve(root, 'content/grammar'),
    levelsYml: resolve(root, 'content/levels.yml'),
    schemaPath: resolve(root, 'scripts/build-content/schema.sql'),
    kanjiDir: resolve(root, 'content/kanji'),
    vocabDir: resolve(root, 'content/vocab'),
    textsDir: resolve(root, 'content/texts'),
    contentVersion: process.env['CONTENT_VERSION'] ?? '0.1.0',
  });
```

- [ ] **Step 6: Update `tests/scripts/build-content.test.ts`**

In the `'content.db schema'` test's expected table list, add `'text_questions'` and `'texts'` in alphabetical order:

```typescript
    expect(tables).toEqual([
      'grammar_examples',
      'grammar_points',
      'grammar_relations',
      'kanji_points',
      'levels',
      'meta',
      'text_questions',
      'texts',
      'vocab_points',
    ]);
```

Add `textsDir` to the shared `opts` object:

```typescript
const opts = {
  grammarDir: resolve(__dirname, '../../content/grammar'),
  levelsYml: resolve(__dirname, '../../content/levels.yml'),
  schemaPath: resolve(__dirname, '../../scripts/build-content/schema.sql'),
  kanjiDir: resolve(__dirname, '../../content/kanji'),
  vocabDir: resolve(__dirname, '../../content/vocab'),
  textsDir: resolve(__dirname, '../../content/texts'),
};
```

Add a new test inside `describe('buildContentDb', ...)`, after the existing `'produces 630 real N4 vocab rows...'` test and before the `'is deterministic...'` test:

```typescript
  it('produces 6 real texts (4 N5, 2 N4) with questions and intact FKs', async () => {
    const bytes = buildContentDb(opts);
    const SQL = await initSqlJs({
      locateFile: () => resolve(__dirname, '../../node_modules/sql.js/dist/sql-wasm.wasm'),
    });
    const db = new SQL.Database(bytes);

    const n5Count = db.exec("SELECT count(*) FROM texts WHERE level = 'N5'")[0]!.values[0]![0];
    expect(n5Count).toBe(4);
    const n4Count = db.exec("SELECT count(*) FROM texts WHERE level = 'N4'")[0]!.values[0]![0];
    expect(n4Count).toBe(2);

    const row = db.exec(
      "SELECT title, body_ruby, translation_ru FROM texts WHERE id = 'n5-kitsune-to-tsuru'",
    )[0]!.values[0]!;
    expect(row[0]).toBe('キツネとツル');
    expect(String(row[1])).toContain('きつね');
    expect(String(row[2])).toContain('журавл');

    const qCount = db.exec(
      "SELECT count(*) FROM text_questions WHERE text_id = 'n5-kitsune-to-tsuru'",
    )[0]!.values[0]![0];
    expect(qCount).toBe(4);

    const firstQ = db.exec(
      "SELECT prompt, choices_json, answer_index FROM text_questions WHERE text_id = 'n5-kitsune-to-tsuru' AND ord = 0",
    )[0]!.values[0]!;
    expect(JSON.parse(String(firstQ[1]))).toHaveLength(4);
    expect(firstQ[2]).toBe(0);

    const orphans = db.exec(`
      SELECT count(*) FROM text_questions q
      LEFT JOIN texts t ON t.id = q.text_id
      WHERE t.id IS NULL
    `)[0]!.values[0]![0];
    expect(orphans).toBe(0);

    db.close();
  });
```

- [ ] **Step 7: Run the failing test to verify it fails first**

Run: `export PATH="$PATH:/c/Program Files/nodejs" && npx vitest run tests/scripts/build-content.test.ts`
Expected: FAIL — `opts` missing `textsDir` (TS) or table list mismatch, until Steps 4-6 are all in place. (If Steps 4-6 are applied together as intended, run this after all of them to confirm the *new* assertions specifically — the table-list and content-count tests.)

- [ ] **Step 8: Run content build and the full test file to verify it passes**

Run: `export PATH="$PATH:/c/Program Files/nodejs" && npm run build-content && npx vitest run tests/scripts/build-content.test.ts`
Expected: PASS — all tests in the file, including the two new/modified ones.

- [ ] **Step 9: Run the full suite and typecheck**

Run: `export PATH="$PATH:/c/Program Files/nodejs" && npx tsc -p tsconfig.json --noEmit && npx vitest run`
Expected: typecheck clean; all tests pass (this also re-runs `tests/storage/content-db.test.ts` against the now-larger `content.db`, which does not yet query `texts`/`text_questions` — that's Task 3 — so it should be unaffected and still pass).

- [ ] **Step 10: Commit**

```bash
git add content/texts content/CREDITS.md scripts/build-content/write-db.ts scripts/build-content/index.ts tests/scripts/build-content.test.ts
git commit -m "feat(texts): author 6 N5/N4 reading texts and wire them into the content pipeline"
```

---

### Task 3: `ContentDb.listTexts` / `getText`

**Files:**
- Modify: `src/storage/content-db.ts`
- Test: `tests/storage/content-db.test.ts`

**Interfaces:**
- Consumes: `TextPoint`/`TextQuestion` (Task 1), `texts`/`text_questions` tables populated by Task 2's real content.
- Produces (used by Task 4/5's screens):
  - `ContentDb.listTexts(level: LevelCode): TextPoint[]` — `bodyRuby`/`translationRu` empty strings, `questions: []` (list-row shape, mirrors `listGrammar`/`listVocab`).
  - `ContentDb.getText(id: string): TextPoint | null` — full row including resolved `questions`.

- [ ] **Step 1: Write the failing tests**

Add to `tests/storage/content-db.test.ts`, at the end of the `describe('ContentDb', ...)` block (after the last existing `it(...)`, before the closing `});`):

```typescript
  it('lists all 4 N5 texts sorted by id', () => {
    const t = db.listTexts('N5');
    expect(t).toHaveLength(4);
    for (let i = 1; i < t.length; i++) expect(t[i]!.id >= t[i - 1]!.id).toBe(true);
    expect(t[0]!.bodyRuby).toBe('');
    expect(t[0]!.questions).toEqual([]);
  });

  it('lists all 2 N4 texts sorted by id, independently of N5', () => {
    const n4 = db.listTexts('N4');
    expect(n4).toHaveLength(2);
    expect(db.listTexts('N5')).toHaveLength(4); // unaffected by N4 landing
  });

  it('gets a full text with body, translation and questions', () => {
    const p = db.getText('n5-kitsune-to-tsuru');
    expect(p).not.toBeNull();
    expect(p!.title).toBe('キツネとツル');
    expect(p!.level).toBe('N5');
    expect(p!.bodyRuby).toContain('きつね');
    expect(p!.translationRu).toContain('журавл');
    expect(p!.questions).toHaveLength(4);
    expect(p!.questions[0]!.choices).toHaveLength(4);
    expect(typeof p!.questions[0]!.answerIndex).toBe('number');
  });

  it('returns null for an unknown text id', () => {
    expect(db.getText('nope')).toBeNull();
  });

  it('gets a full N4 text', () => {
    const p = db.getText('n4-kin-no-ono-gin-no-ono');
    expect(p).not.toBeNull();
    expect(p!.level).toBe('N4');
    expect(p!.questions.length).toBeGreaterThanOrEqual(3);
  });
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `export PATH="$PATH:/c/Program Files/nodejs" && npx vitest run tests/storage/content-db.test.ts`
Expected: FAIL — `db.listTexts is not a function`.

- [ ] **Step 3: Implement `listTexts`/`getText` in `src/storage/content-db.ts`**

Add the import of `TextPoint` to the existing type-only import line at the top of the file:

```typescript
import type { GrammarPoint, KanjiPoint, Level, LevelCode, TextPoint, VocabPoint } from '@/core/types';
```

Add row interfaces near the other row interfaces (after `VocabRow`):

```typescript
interface TextListRow {
  id: string;
  level: string;
  title: string;
}

interface TextRow extends TextListRow {
  body_ruby: string;
  translation_ru: string;
}

interface TextQuestionRow {
  prompt: string;
  choices_json: string;
  answer_index: number;
}
```

Add a column constant near `LIST_COLS`/`FULL_COLS`:

```typescript
const TEXT_LIST_COLS = 'id, level, title';
```

Add the methods to the `ContentDb` class, after the existing `searchVocab` method (before the closing `}` of the class):

```typescript
  private rowToText(r: TextListRow): TextPoint {
    return {
      id: r.id,
      level: r.level,
      title: r.title,
      bodyRuby: '',
      translationRu: '',
      questions: [],
    };
  }

  listTexts(level: LevelCode): TextPoint[] {
    return this.all<TextListRow>(
      `SELECT ${TEXT_LIST_COLS} FROM texts WHERE level = ? ORDER BY id`,
      [level],
    ).map((r) => this.rowToText(r));
  }

  getText(id: string): TextPoint | null {
    const rows = this.all<TextRow>(
      `SELECT ${TEXT_LIST_COLS}, body_ruby, translation_ru FROM texts WHERE id = ?`,
      [id],
    );
    const row = rows[0];
    if (!row) return null;
    const questions = this.all<TextQuestionRow>(
      'SELECT prompt, choices_json, answer_index FROM text_questions WHERE text_id = ? ORDER BY ord',
      [id],
    ).map((q) => ({
      prompt: q.prompt,
      choices: JSON.parse(q.choices_json) as string[],
      answerIndex: q.answer_index,
    }));
    return {
      id: row.id,
      level: row.level,
      title: row.title,
      bodyRuby: row.body_ruby,
      translationRu: row.translation_ru,
      questions,
    };
  }
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `export PATH="$PATH:/c/Program Files/nodejs" && npm run build-content && npx vitest run tests/storage/content-db.test.ts`
Expected: PASS — 5 new tests plus the pre-existing ones.

- [ ] **Step 5: Run full suite + typecheck**

Run: `export PATH="$PATH:/c/Program Files/nodejs" && npx tsc -p tsconfig.json --noEmit && npx vitest run`
Expected: clean.

- [ ] **Step 6: Commit**

```bash
git add src/storage/content-db.ts tests/storage/content-db.test.ts
git commit -m "feat(texts): add ContentDb.listTexts/getText"
```

---

### Task 4: `TextsListScreen`, Nav entry, `/texts` route

**Files:**
- Create: `src/ui/screens/TextsListScreen.tsx`
- Modify: `src/ui/components/Nav.tsx`
- Modify: `src/ui/routes.tsx`
- Modify: `src/ui/theme.css`
- Test: `tests/ui/TextsListScreen.test.tsx`
- Modify: `tests/ui/Nav.test.tsx`

**Interfaces:**
- Consumes: `useContentDb`/`useLevels` (`src/ui/useContentDb.ts`), `useUserDb` (`src/ui/useUserDb.ts`, for the `texts_read_ids` setting), `ContentDb.listTexts` (Task 3).
- Produces: `TextsListScreen` component (default export style matches sibling screens — named export `TextsListScreen`), route `/texts`, Nav destination linking to it.

- [ ] **Step 1: Write the failing test file**

Create `tests/ui/TextsListScreen.test.tsx`:

```tsx
import '@testing-library/jest-dom/vitest';
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, fireEvent } from '@testing-library/react';
import { MemoryRouter as BaseMemoryRouter } from 'react-router-dom';
import type { ComponentProps } from 'react';
import { ContentDbContext } from '@/ui/ContentDbProvider';

const FUTURE = { v7_startTransition: true, v7_relativeSplatPath: true } as const;
const MemoryRouter = (props: ComponentProps<typeof BaseMemoryRouter>) => (
  <BaseMemoryRouter future={FUTURE} {...props} />
);

const readIds: { value: string[] } = { value: [] };
vi.mock('@/ui/useUserDb', () => ({
  useUserDb: () => ({ getSetting: () => readIds.value }),
}));

import { TextsListScreen } from '@/ui/screens/TextsListScreen';
import type { TextPoint, Level } from '@/core/types';

const levels: Level[] = [
  { code: 'N5', ord: 1, status: 'available', titleRu: 'N5' },
  { code: 'N4', ord: 2, status: 'coming_soon', titleRu: 'N4' },
];
const n5: TextPoint[] = [
  { id: 'n5-a', level: 'N5', title: 'キツネとツル', bodyRuby: '', translationRu: '', questions: [] },
  { id: 'n5-b', level: 'N5', title: '二匹のかえる', bodyRuby: '', translationRu: '', questions: [] },
];
const fakeDb = {
  listTexts: (l: string) => (l === 'N5' ? n5 : []),
} as unknown as import('@/storage/content-db').ContentDb;

function renderScreen() {
  return render(
    <ContentDbContext.Provider value={{ db: fakeDb, levels }}>
      <MemoryRouter><TextsListScreen /></MemoryRouter>
    </ContentDbContext.Provider>,
  );
}

describe('TextsListScreen', () => {
  beforeEach(() => {
    readIds.value = [];
  });

  it('shows N5 texts by default', () => {
    const { getAllByRole } = renderScreen();
    const links = getAllByRole('link').filter((a) => a.getAttribute('href')?.includes('/texts/'));
    expect(links).toHaveLength(2);
    expect(links[0]!).toHaveTextContent('キツネとツル');
  });

  it('marks a coming-soon level as empty', () => {
    const { getByRole, getByText } = renderScreen();
    fireEvent.click(getByRole('tab', { name: /N4/ }));
    expect(getByText(/скоро/i)).toBeInTheDocument();
  });

  it('shows no read badge for an unread text', () => {
    const { container } = renderScreen();
    expect(container.querySelector('.text-read-badge')).toBeNull();
  });

  it('shows a read badge only for a text whose id is in texts_read_ids', () => {
    readIds.value = ['n5-a'];
    const { container, getAllByRole } = renderScreen();
    const links = getAllByRole('link').filter((a) => a.getAttribute('href')?.includes('/texts/'));
    expect(links[0]!.querySelector('.text-read-badge')).not.toBeNull();
    expect(links[1]!.querySelector('.text-read-badge')).toBeNull();
    expect(container.querySelectorAll('.text-read-badge')).toHaveLength(1);
  });
});
```

Also modify `tests/ui/Nav.test.tsx`'s last test to expect six destinations:

```typescript
  it('lists the six primary destinations', () => {
    const { getByRole } = render(<MemoryRouter><Nav /></MemoryRouter>);
    for (const label of ['Сегодня', 'Грамматика', 'Кандзи', 'Слова', 'Тексты', 'Прогресс']) {
      expect(getByRole('link', { name: new RegExp(label) })).toBeTruthy();
    }
  });
```

- [ ] **Step 2: Run the tests to verify they fail**

Run: `export PATH="$PATH:/c/Program Files/nodejs" && npx vitest run tests/ui/TextsListScreen.test.tsx tests/ui/Nav.test.tsx`
Expected: FAIL — `Cannot find module '@/ui/screens/TextsListScreen'`; Nav test fails on the missing "Тексты" link.

- [ ] **Step 3: Implement `src/ui/screens/TextsListScreen.tsx`**

```tsx
import { useMemo, useState } from 'react';
import { Link } from 'react-router-dom';
import { useContentDb, useLevels } from '../useContentDb';
import { useUserDb } from '../useUserDb';

export function TextsListScreen() {
  const db = useContentDb();
  const levels = useLevels();
  const user = useUserDb();
  const [activeLevel, setActiveLevel] = useState(levels[0]?.code ?? '');

  const activeLevelObj = levels.find((l) => l.code === activeLevel);
  const points = useMemo(() => db.listTexts(activeLevel), [db, activeLevel]);
  const readIds = user.getSetting<string[]>('texts_read_ids', []);

  return (
    <section className="screen">
      <h1>Тексты</h1>
      <div role="tablist" className="level-tabs">
        {levels.map((l) => (
          <button
            key={l.code}
            role="tab"
            type="button"
            aria-selected={l.code === activeLevel}
            className="level-tab"
            onClick={() => setActiveLevel(l.code)}
          >
            {l.code}
          </button>
        ))}
      </div>
      {activeLevelObj?.status === 'coming_soon' ? (
        <p className="muted">Материал уровня {activeLevel} появится скоро.</p>
      ) : points.length === 0 ? (
        <p className="muted">Текстов пока нет.</p>
      ) : (
        <ul className="text-list">
          {points.map((p) => (
            <li key={p.id}>
              <Link to={`/texts/${p.id}`} className="text-list-item">
                <span className="text-list-title">{p.title}</span>
                {readIds.includes(p.id) ? (
                  <span className="text-read-badge" aria-label="прочитано">✓</span>
                ) : null}
              </Link>
            </li>
          ))}
        </ul>
      )}
    </section>
  );
}
```

- [ ] **Step 4: Wire the `/texts` route**

In `src/ui/routes.tsx`, add the import (after the `VocabDetailScreen` import):

```typescript
import { TextsListScreen } from './screens/TextsListScreen';
```

Add the route entry (after `/vocab/:id`, before `/progress`):

```typescript
  { path: '/texts', element: <TextsListScreen /> },
```

(Task 5 adds `/texts/:id` right after this line — leaving it absent for now means a click on a text card 404s within this task's scope only; that's an acceptable, short-lived intermediate state within the plan, resolved by the very next task, and does not affect this task's own tests, which never navigate past the list.)

- [ ] **Step 5: Add the "Тексты" Nav destination**

In `src/ui/components/Nav.tsx`, update `DESTINATIONS`:

```typescript
const DESTINATIONS = [
  { to: '/', label: 'Сегодня', icon: '🗓' },
  { to: '/grammar', label: 'Грамматика', icon: '📘' },
  { to: '/kanji', label: 'Кандзи', icon: '㊗' },
  { to: '/vocab', label: 'Слова', icon: '📝' },
  { to: '/texts', label: 'Тексты', icon: '📖' },
  { to: '/progress', label: 'Прогресс', icon: '📈' },
];
```

- [ ] **Step 6: Add CSS rules to `src/ui/theme.css`**

Append at the end of the file:

```css
/* --- Plan 4c-2: texts --- */
.text-list { list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 6px; }
.text-list-item { display: flex; align-items: center; gap: 8px; padding: 10px 12px;
  border: 1px solid var(--border); border-radius: 8px; text-decoration: none; color: var(--text); }
.text-list-title { flex: 1; font-weight: 500; }
.text-read-badge { color: var(--ok); font-weight: 700; }
```

- [ ] **Step 7: Run the tests to verify they pass**

Run: `export PATH="$PATH:/c/Program Files/nodejs" && npx vitest run tests/ui/TextsListScreen.test.tsx tests/ui/Nav.test.tsx`
Expected: PASS.

- [ ] **Step 8: Run full suite + typecheck**

Run: `export PATH="$PATH:/c/Program Files/nodejs" && npx tsc -p tsconfig.json --noEmit && npx vitest run`
Expected: clean — confirms no other test broke from the Nav destination list change (the earlier `Grep` in Global Constraints found no other test asserting Nav link counts).

- [ ] **Step 9: Commit**

```bash
git add src/ui/screens/TextsListScreen.tsx src/ui/components/Nav.tsx src/ui/routes.tsx src/ui/theme.css tests/ui/TextsListScreen.test.tsx tests/ui/Nav.test.tsx
git commit -m "feat(texts): add TextsListScreen, Nav entry, and /texts route"
```

---

### Task 5: `TextDetailScreen`, `/texts/:id` route, comprehension-question flow, read tracking

**Files:**
- Create: `src/ui/screens/TextDetailScreen.tsx`
- Modify: `src/ui/routes.tsx`
- Modify: `src/ui/theme.css`
- Test: `tests/ui/TextDetailScreen.test.tsx`

**Interfaces:**
- Consumes: `useContentDb` (`ContentDb.getText`, Task 3), `useUserDb` (`getSetting`/`setSetting`), `Furigana` (`src/ui/components/Furigana.tsx`, unchanged — reused as-is for the `## Текст` paragraphs), `TextPoint`/`TextQuestion` (Task 1).
- Produces: `TextDetailScreen` component, route `/texts/:id`, the `texts_read_ids: string[]` user setting (read by Task 4's `TextsListScreen`).

- [ ] **Step 1: Write the failing test file**

Create `tests/ui/TextDetailScreen.test.tsx`:

```tsx
import '@testing-library/jest-dom/vitest';
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { MemoryRouter as BaseMemoryRouter, Routes, Route } from 'react-router-dom';
import type { ComponentProps } from 'react';
import { ContentDbContext } from '@/ui/ContentDbProvider';

const FUTURE = { v7_startTransition: true, v7_relativeSplatPath: true } as const;
const MemoryRouter = (props: ComponentProps<typeof BaseMemoryRouter>) => (
  <BaseMemoryRouter future={FUTURE} {...props} />
);

const furiganaSetting: { value: boolean } = { value: true };
const readIds: { value: string[] } = { value: [] };
const setSetting = vi.fn((key: string, value: unknown) => {
  if (key === 'texts_read_ids') readIds.value = value as string[];
});
vi.mock('@/ui/useUserDb', () => ({
  useUserDb: () => ({
    getSetting: (key: string, fallback: unknown) => {
      if (key === 'furigana_enabled') return furiganaSetting.value;
      if (key === 'texts_read_ids') return readIds.value;
      return fallback;
    },
    setSetting,
  }),
}));

import { TextDetailScreen } from '@/ui/screens/TextDetailScreen';
import type { Level, TextPoint } from '@/core/types';

const levels: Level[] = [{ code: 'N5', ord: 1, status: 'available', titleRu: 'N5' }];
const sample: TextPoint = {
  id: 'n5-sample',
  level: 'N5',
  title: 'サンプル',
  bodyRuby: 'これは 文[ぶん]です。\n\n二[ふた]つ目[め]の 段落[だんらく]です。',
  translationRu: 'Это предложение.\n\nВторой абзац.',
  questions: [
    { prompt: 'Вопрос 1?', choices: ['A', 'B', 'C'], answerIndex: 1 },
    { prompt: 'Вопрос 2?', choices: ['D', 'E', 'F'], answerIndex: 0 },
  ],
};
const fakeDb = {
  getText: (id: string) => (id === sample.id ? sample : null),
} as unknown as import('@/storage/content-db').ContentDb;

function renderAt(path: string) {
  return render(
    <ContentDbContext.Provider value={{ db: fakeDb, levels }}>
      <MemoryRouter initialEntries={[path]}>
        <Routes>
          <Route path="/texts/:id" element={<TextDetailScreen />} />
        </Routes>
      </MemoryRouter>
    </ContentDbContext.Provider>,
  );
}

describe('TextDetailScreen', () => {
  beforeEach(() => {
    furiganaSetting.value = true;
    readIds.value = [];
    setSetting.mockClear();
  });

  it('renders the title and both paragraphs with furigana', () => {
    const { container } = renderAt('/texts/n5-sample');
    expect(screen.getByRole('heading', { name: 'サンプル' })).toBeInTheDocument();
    expect(container.querySelectorAll('.text-paragraph')).toHaveLength(2);
    expect(container.querySelector('ruby')).not.toBeNull();
  });

  it('shows a not-found message for an unknown id', () => {
    renderAt('/texts/does-not-exist');
    expect(screen.getByText(/не найден/i)).toBeInTheDocument();
  });

  it('reveals the translation only after clicking the button', () => {
    renderAt('/texts/n5-sample');
    expect(screen.queryByText('Это предложение.')).toBeNull();
    fireEvent.click(screen.getByRole('button', { name: 'Показать перевод' }));
    expect(screen.getByText('Это предложение.')).toBeInTheDocument();
    expect(screen.getByText('Второй абзац.')).toBeInTheDocument();
  });

  it('walks through questions one at a time and marks the text as read on completion', () => {
    renderAt('/texts/n5-sample');
    expect(screen.getByText('Вопрос 1?')).toBeInTheDocument();

    fireEvent.click(screen.getByRole('button', { name: 'B' }));
    fireEvent.click(screen.getByRole('button', { name: 'Далее' }));

    expect(screen.getByText('Вопрос 2?')).toBeInTheDocument();
    fireEvent.click(screen.getByRole('button', { name: 'D' }));
    fireEvent.click(screen.getByRole('button', { name: 'Завершить' }));

    expect(screen.getByText(/Текст прочитан/)).toBeInTheDocument();
    expect(setSetting).toHaveBeenCalledWith('texts_read_ids', ['n5-sample']);
  });

  it('does not add a duplicate entry when the text id is already in texts_read_ids', () => {
    readIds.value = ['n5-sample'];
    renderAt('/texts/n5-sample');
    fireEvent.click(screen.getByRole('button', { name: 'B' }));
    fireEvent.click(screen.getByRole('button', { name: 'Далее' }));
    fireEvent.click(screen.getByRole('button', { name: 'D' }));
    fireEvent.click(screen.getByRole('button', { name: 'Завершить' }));
    expect(setSetting).not.toHaveBeenCalledWith('texts_read_ids', expect.anything());
  });

  it('highlights the correct and wrong choice after answering', () => {
    const { container } = renderAt('/texts/n5-sample');
    fireEvent.click(screen.getByRole('button', { name: 'A' })); // wrong, correct is B
    const opts = container.querySelectorAll('.q-opt');
    expect(opts[0]).toHaveClass('opt-wrong');
    expect(opts[1]).toHaveClass('opt-correct');
  });

  it('links back to the texts list', () => {
    renderAt('/texts/n5-sample');
    expect(screen.getByRole('link', { name: /тексты/i })).toHaveAttribute('href', '/texts');
  });
});
```

- [ ] **Step 2: Run the tests to verify they fail**

Run: `export PATH="$PATH:/c/Program Files/nodejs" && npx vitest run tests/ui/TextDetailScreen.test.tsx`
Expected: FAIL — `Cannot find module '@/ui/screens/TextDetailScreen'`.

- [ ] **Step 3: Implement `src/ui/screens/TextDetailScreen.tsx`**

```tsx
import { useEffect, useMemo, useRef, useState } from 'react';
import { Link, useParams } from 'react-router-dom';
import { useContentDb } from '../useContentDb';
import { useUserDb } from '../useUserDb';
import { Furigana } from '../components/Furigana';

export function TextDetailScreen() {
  const { id = '' } = useParams();
  const db = useContentDb();
  const user = useUserDb();
  const point = useMemo(() => db.getText(id), [db, id]);

  const [translationShown, setTranslationShown] = useState(false);
  const [qIndex, setQIndex] = useState(0);
  const [selected, setSelected] = useState<number | null>(null);
  const markedRef = useRef(false);

  const done = point !== null && qIndex >= point.questions.length;

  useEffect(() => {
    if (!point || !done || markedRef.current) return;
    markedRef.current = true;
    const existing = user.getSetting<string[]>('texts_read_ids', []);
    if (!existing.includes(point.id)) {
      user.setSetting('texts_read_ids', [...existing, point.id]);
    }
  }, [point, done, user]);

  if (!point) {
    return (
      <section className="screen">
        <p className="muted">Текст не найден.</p>
        <Link to="/texts" className="back-link">
          ← Тексты
        </Link>
      </section>
    );
  }

  const paragraphs = point.bodyRuby.split('\n\n');
  const translationParagraphs = point.translationRu.split('\n\n');
  const question = point.questions[qIndex];

  const choose = (i: number) => {
    if (selected !== null) return;
    setSelected(i);
  };

  const next = () => {
    setQIndex((n) => n + 1);
    setSelected(null);
  };

  return (
    <section className="screen text-detail">
      <Link to="/texts" className="back-link">
        ← Тексты
      </Link>
      <h1>{point.title}</h1>

      <div className="text-body">
        {paragraphs.map((p, i) => (
          <p key={i} className="text-paragraph">
            <Furigana text={p} />
          </p>
        ))}
      </div>

      <button
        type="button"
        className="btn-ghost"
        onClick={() => setTranslationShown((s) => !s)}
      >
        {translationShown ? 'Скрыть перевод' : 'Показать перевод'}
      </button>
      {translationShown && (
        <div className="text-translation">
          {translationParagraphs.map((p, i) => (
            <p key={i}>{p}</p>
          ))}
        </div>
      )}

      {!done && question && (
        <div className="q text-question">
          <p className="q-prompt">{question.prompt}</p>
          <div className="q-options">
            {question.choices.map((choice, i) => {
              let cls = 'q-opt';
              if (selected !== null) {
                if (i === question.answerIndex) cls += ' opt-correct';
                else if (i === selected) cls += ' opt-wrong';
              }
              return (
                <button
                  key={i}
                  type="button"
                  className={cls}
                  disabled={selected !== null}
                  onClick={() => choose(i)}
                >
                  {choice}
                </button>
              );
            })}
          </div>
          {selected !== null && (
            <button type="button" className="btn-primary" onClick={next}>
              {qIndex + 1 < point.questions.length ? 'Далее' : 'Завершить'}
            </button>
          )}
        </div>
      )}

      {done && (
        <p className="text-done">Текст прочитан. Вопросы пройдены: {point.questions.length}.</p>
      )}
    </section>
  );
}
```

- [ ] **Step 4: Wire the `/texts/:id` route**

In `src/ui/routes.tsx`, add the import (after `TextsListScreen`):

```typescript
import { TextDetailScreen } from './screens/TextDetailScreen';
```

Add the route entry right after `/texts`:

```typescript
  { path: '/texts/:id', element: <TextDetailScreen /> },
```

- [ ] **Step 5: Add CSS rules to `src/ui/theme.css`**

Append after the Task 4 `.text-*` rules:

```css
.text-detail { max-width: 40rem; }
.text-body { font-family: var(--font-ja); line-height: 2; margin: 1rem 0; }
.text-paragraph { margin: 0 0 1em; }
.text-translation { color: var(--muted); margin: 1rem 0; }
.text-translation p { margin: 0 0 0.6em; }
.text-question { margin-top: 1.5rem; padding-top: 1rem; border-top: 1px solid var(--border); }
.text-done { margin-top: 1.5rem; font-size: 1.05rem; color: var(--ok); font-weight: 600; }
```

- [ ] **Step 6: Run the tests to verify they pass**

Run: `export PATH="$PATH:/c/Program Files/nodejs" && npx vitest run tests/ui/TextDetailScreen.test.tsx`
Expected: PASS — 7 tests.

- [ ] **Step 7: Run full suite + typecheck**

Run: `export PATH="$PATH:/c/Program Files/nodejs" && npx tsc -p tsconfig.json --noEmit && npx vitest run`
Expected: clean.

- [ ] **Step 8: Commit**

```bash
git add src/ui/screens/TextDetailScreen.tsx src/ui/routes.tsx src/ui/theme.css tests/ui/TextDetailScreen.test.tsx
git commit -m "feat(texts): add TextDetailScreen with comprehension questions and read tracking"
```

---

### Task 6: e2e coverage and final regression

**Files:**
- Create: `tests/e2e/texts-browse.spec.ts`

**Interfaces:**
- Consumes: the fully wired `/texts`, `/texts/:id` routes and `TextsListScreen`/`TextDetailScreen` from Tasks 4-5, run against the packaged Electron app (`out/main/main.js`), mirroring `tests/e2e/vocab-browse.spec.ts`.

- [ ] **Step 1: Write the e2e test file**

Create `tests/e2e/texts-browse.spec.ts`:

```typescript
import { test, expect, _electron as electron } from '@playwright/test';

test('browse texts: list -> detail -> reveal translation -> answer questions -> read badge', async () => {
  const app = await electron.launch({ args: ['out/main/main.js'] });
  const win = await app.firstWindow();

  await win.getByRole('link', { name: /Тексты/ }).click();
  await expect(win.getByRole('heading', { name: 'Тексты' })).toBeVisible();

  const items = win.locator('.text-list-item');
  await expect(items).toHaveCount(4); // 4 N5 texts

  await win.getByRole('link', { name: /キツネとツル/ }).click();
  await expect(win.getByRole('heading', { name: 'キツネとツル' })).toBeVisible();

  await win.getByRole('button', { name: 'Показать перевод' }).click();
  await expect(win.getByText(/Давным-давно/)).toBeVisible();

  // Answer all 4 comprehension questions, always picking the first choice,
  // then advancing -- the point is to reach the "read" end state, not to
  // score correctly.
  for (let i = 0; i < 4; i++) {
    await win.locator('.q-opt').first().click();
    await win.getByRole('button', { name: /Далее|Завершить/ }).click();
  }

  await expect(win.getByText(/Текст прочитан/)).toBeVisible();

  await win.locator('.back-link').click();
  await expect(win.getByRole('heading', { name: 'Тексты' })).toBeVisible();
  await expect(win.locator('.text-read-badge')).toHaveCount(1);

  await app.close();
});

test('switching to the N4 tab shows N4 texts', async () => {
  const app = await electron.launch({ args: ['out/main/main.js'] });
  const win = await app.firstWindow();

  await win.getByRole('link', { name: /Тексты/ }).click();
  await win.getByRole('tab', { name: 'N4' }).click();
  await expect(win.locator('.text-list-item')).toHaveCount(2);

  await app.close();
});
```

- [ ] **Step 2: Build and run the new e2e spec in isolation**

Run:
```bash
export PATH="$PATH:/c/Program Files/nodejs" && npm run build:desktop && npx playwright test tests/e2e/texts-browse.spec.ts
```
Expected: both tests PASS. If the "read badge" assertion fails because a previous run's `user.db` in a shared Electron user-data dir already has `n5-kitsune-to-tsuru` in `texts_read_ids`, check whether other e2e specs in this suite reset `user.db` per-test (per `tests/e2e/helpers/seed-user-db.ts` if present) and apply the same isolation this suite already relies on elsewhere — do not weaken the assertion instead of fixing isolation, since read-badge persistence is exactly the behavior under test.

- [ ] **Step 3: Full final regression**

Run, in order, stopping to fix forward on any failure before proceeding to the next command:

```bash
export PATH="$PATH:/c/Program Files/nodejs" && npm run typecheck
export PATH="$PATH:/c/Program Files/nodejs" && npm run lint
export PATH="$PATH:/c/Program Files/nodejs" && npx vitest run
export PATH="$PATH:/c/Program Files/nodejs" && npm run build:desktop
export PATH="$PATH:/c/Program Files/nodejs" && npx playwright test
```

Expected: `typecheck` 0 errors; `lint` 0 errors; `vitest run` all tests pass (record the exact new total — starting baseline was 258, this plan added roughly 17 (Task 1) + 1 (Task 2's new test) + 5 (Task 3) + 6 (Task 4: 4 new + 2 modified-not-added) + 7 (Task 5) ≈ 30-36 new tests; state the real number from the terminal, don't reuse this estimate); `build:desktop` succeeds; `playwright test` all pass (record the exact new total — starting baseline was 23, this plan adds 2, so 25, but confirm against real output).

- [ ] **Step 4: Commit**

```bash
git add tests/e2e/texts-browse.spec.ts
git commit -m "test(texts): add e2e coverage for browsing, reading, and completing a text"
```

---

## Self-Review Notes (per writing-plans skill)

- **Spec coverage:** §2 (content format, schema, parser, "3-4 questions, exactly one `[x]`, validated") → Task 1 + Task 2. §3 (`/texts` list mirroring `VocabListScreen` with read badges; `/texts/:id` with furigana/translation-toggle/sequential questions/`texts_read_ids`; Nav entry) → Tasks 4-5. §4 (out of scope: tap-word lookup, TTS, SRS queue, N3-N1 content) → explicitly excluded throughout, called out in Global Constraints. Tadoku sourcing + `CREDITS.md` attribution (task prompt, not in the design doc itself) → Task 2.
- **Placeholder scan:** every task's code steps are complete, runnable snippets; no "similar to Task N", no "add validation" hand-waving — validation logic is fully written out in Task 1's `validateTexts`/`parseQuestions`.
- **Type consistency:** `TextPoint`/`TextQuestion` (Task 1) are used with identical field names (`bodyRuby`, `translationRu`, `questions`, `prompt`, `choices`, `answerIndex`) across `parse-texts.ts`, `write-db.ts`, `content-db.ts`, `TextsListScreen.tsx`, and `TextDetailScreen.tsx` — verified by re-reading each task's code side by side while writing this plan. `listTexts`/`getText` names match between Task 3's production code and Tasks 4-5's screen code and tests.
